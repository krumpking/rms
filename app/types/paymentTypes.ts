export type IPayments = {
    id: string,
    adminId: string,
    userId: string,
    date: Date,
    dateString: string,
    amount: number,
    duration: number,
    refCode: string,
    package: string
}
